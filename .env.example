# GitHub API token — optional, raises rate limit for /api/latest-repo
# Create a fine-grained or classic token at https://github.com/settings/tokens
# No scopes needed for public repo/PR reads. Without it the API falls back
# to unauthenticated requests and the site still works (profile URL fallback).
GITHUB_TOKEN=
